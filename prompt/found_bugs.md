# Found bugs

### Description:

This file contains bugs discovered during manual testing that don't significantly impact system operation. Therefore, fixing them is postponed.

### Bugs list:

- When first comment added to article, then it shows together with sentence 'Пока нет комментариев'
- When some changes in article posted, we redirect user to article showing page without dispatch. Result is that if user want to reload page, he re-posted last changes.